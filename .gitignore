**/__pycache__/

# Full-size processed datasets — regenerate locally, or see README for download link.
# Small samples (*_sample.csv) are kept in git so the repo structure is inspectable.
data/processed/spy_processed.csv
data/processed/spy_sentiment_processed.csv

# Raw intraday price data is large and excluded — see README for source.
# sentiment_analysis.csv is small enough to keep as-is.
data/raw/*.xlsx

# Old/duplicate model checkpoints — only the best model is kept under Lstm/saved_models/
Lstm/saved_models/archive/
Lstm/saved_models/logs/
