**/__pycache__/

# Data is not included in this repo (see README's Data section for why and
# what's expected locally to run the training scripts).
data/

# Old/duplicate model checkpoints — only the best model is kept under Lstm/saved_models/
Lstm/saved_models/archive/
Lstm/saved_models/logs/
